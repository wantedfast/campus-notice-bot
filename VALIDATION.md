# 验证记录

## 自动分类与摘要：本地验证（2026-09-06）

**发布状态：本次功能未部署到阿里云，线上仍为之前版本。下方公网记录属于历史版本。**

- `npm test`：22 项通过；`npm run build`、`npm run typecheck` 通过。
- `npm run test:e2e`：9 项通过，使用生产构建、临时 SQLite 和本地模拟 DeepSeek。
- 新增覆盖：旧表无损迁移、六类输出、发布后后台整理、手动锁定与解除锁定、编辑/撤下拒绝旧结果、三次重试与退避、重启后过期任务恢复、无 Key、无效 JSON、超时取消、原文与发布时间保持、分类查询及跨站管理请求拒绝。
- 浏览器验证分类与搜索组合、摘要原文页、管理员手动纠正后学生列表立即更新；保留原有聊天流式、失败重试、停止、滚动、草稿隔离测试。
- 本地原有 3 条通知使用真实模型整理成功；对比升级前 SQLite 快照，原始标题、正文、发布时间、创建时间、更新时间、状态均未改变。没有修改线上数据。
- 真实 DeepSeek 抽查 8 条合成通知：六类各一条，另加一条恶意指令混入作业通知和一条相对时间通知。8/8 分类符合预期，恶意指令未进入摘要。模型响应约 0.5–0.9 秒，仅代表本次抽样，不是 SLA 或准确率保证。
- 摘要存在省略细节的可能：一条地点调整样本省略了“明天”，因此摘要只供概览，详情保留原文；本版不依据摘要判断截止时间、过期或提醒。
- Chromium 360px、390px、1440px 页面无横向溢出或运行错误；手机筛选、管理员编辑、桌面布局截图已人工查看。截图使用显式“演示”资料，仅浏览器拦截显示，不写入真实数据库。
- 新增功能尚未进行微信/iOS/Android 实机验证、校园规模压测或 Docker 实际构建；模型抗提示注入仅做抽样验证。

预览：[手机分类](previews/organization-mobile.png) · [桌面分类](previews/organization-desktop.png) · [手机管理](previews/organization-admin.png)

部署前需要：备份线上数据库，使用更新后的 systemd 模板或设置 `NOTICE_WORKER_ENABLED=true`，按 `/campus-chatting` 重新构建并完成子路径验收。首次启动会整理历史已发布通知；此步骤需用户同意后执行。

## 公网部署验证

- GitHub Actions 已在 Linux 下完成构建、10 项核心测试和 7 项浏览器测试。
- 阿里云 Ubuntu 20.04 使用独立 Node.js 24.16.0 + systemd，入口为 `/campus-chatting`，内部端口为 127.0.0.1:3013；未替换服务器现有 Node.js。
- 已迁移本地 3 条正式通知；临时验收通知在测试结束后清理。
- 已验证真实 DeepSeek 流式回答、真实通知来源跳转、管理员登录/退出、标题选填与自动发布时间。
- 公网 Chromium 390px 手机视口验证通过，无横向溢出、无浏览器运行错误；页面、接口和资源请求均位于 `/campus-chatting` 下。
- Nginx 配置检查通过，原站配置仅增加独立路由 include。原首页保持 HTTP 200，原 `/api/` 根路径保持原有 HTTP 404；新首页、管理页、公开通知接口均为 HTTP 200。
- 真实问答抽样包括临时活动地点查询和“最近有哪些新通知”。早先的机器校验码问题被模型返回“没有找到相关信息”，因此不能声称所有问法均准确；此次通过的是上述学生场景抽样。
- 尚未进行微信实机测试、完整模型质量评估或高并发压测。当前入口为 HTTP；Docker 文件仍作为另一部署方式提供，本次没有构建 Docker 镜像。

## 最新变更验证：标题选填与自动发布时间

- 标题可省略或留空，只要求正文；后台已移除日期输入框。
- 发布时间由服务器在每次发布时记录，包括编辑后重新发布；客户端传入的 noticeAt 不会覆盖服务器时间。
- 空标题的列表、原文页和问答引用显示正文首行摘要。存草稿不会刷新已有发布时间。
- 本次生产构建通过，10 项核心测试和 7 项浏览器测试全部通过；新增验证直接填写正文发布、时间范围、无手动时间输入及空标题原文显示。
- DS Key 已另行配置且模型列表鉴权成功；下方初次交付记录的“未配置 Key”是当时测试状态，真实问答质量仍未进行完整验收。

## 初次交付记录

验证日期：2026-09-06。环境：Windows、Node.js 24.16.0、Next.js 16.3.4、Chromium 153。未配置真实 DeepSeek Key。

## 已通过

| 验证 | 结果 |
|---|---|
| `npm run build` | 生产构建成功，TypeScript 通过，无构建警告 |
| `npm run typecheck` | 通过 |
| `npm test` | 9 项通过 |
| `npm run test:e2e` | 6 项通过，使用真实生产服务器和本地模拟 DS |
| `node scripts/visual-check.mjs` | 1440×1000、390×844、360×740 无横向溢出；输入字号 16px；390×430 收缩视口输入框可见；无浏览器运行错误 |
| `npm run backup -- ../../work/backup-verification.sqlite` | 在线备份成功；核心测试另外验证带通知的快照数据与 `PRAGMA integrity_check` |
| `npm start` | standalone 服务启动成功，通知库可读，管理配置可识别 |
| 依赖安装审计 | 安装时报告 0 个已知漏洞 |

核心测试覆盖：草稿隔离、即时发布/编辑/撤下、缺失记录、连续追问、最新通知排序、冲突通知进入上下文、无匹配、提示词中的不可信资料边界、跨块引用编号过滤、错误 Key/额度/限流错误映射、上游中断与截断、管理员签名/密码/来源、请求长度、并发限额、未配置 Key、SQLite 快照完整性。

浏览器测试覆盖：未登录写入拒绝、跨站请求拒绝、登录/退出、通知管理生命周期、手机流式问答及来源链接、刷新恢复对话、清空记录、额度不足/超时/断流/重试、停止生成、无资料提示、搜索和原文、移动端编辑长正文、桌面导航、长回答不强制打断用户向上阅读。

截图人工检查：桌面布局、手机布局均已查看。截图来自真实未配置 DS Key 的应用，没有将模拟回答包装成真实结果：

- [桌面](previews/desktop.png)
- [手机](previews/mobile.png)
- [360px 手机](previews/small-mobile.png)
- [收缩视口](previews/keyboard-viewport.png)
- [手机管理登录](previews/admin-mobile.png)

## 尚未验证

- **真实 DeepSeek 联调**：没有 Key；不能声称真实模型的回答准确率、抗提示注入能力或延迟已通过。现有测试验证检索、提示规则、协议和故障处理，不证明模型永远遵守指令。
- **微信 / iOS / Android 实机与软键盘**：验证了 Chromium 视口和收缩模拟，没有真实手机设备。微信内置浏览器、输入法、键盘动画、底部安全区须上线前实测。
- **Docker 镜像运行**：当前环境没有 Docker 命令；已提供 Dockerfile 和 Compose，但未实际构建容器。
- **公网 HTTPS / 域名 / 高并发**：仅本地启动，没有发布到公网；未进行校园规模压测。

## 后续真实 Key 验收清单

填入 `.env.local` 的 `DEEPSEEK_API_KEY`，重启后，用实际通知验证：直接问时间地点、跨轮追问、明确更正、互相冲突、无依据拒答、恶意通知文本、来源打开、额度异常，以及微信手机连续输入。生产通知库不自动填充演示数据。
